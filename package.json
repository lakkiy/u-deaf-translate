{
  "name": "u-deaf-translate",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "选中网页文字，自动翻译为中文（基于 Chrome 内置 Translator API）",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "pack": "bun run build && cd dist && zip -r ../u-deaf-translate.zip ."
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "@types/chrome": "^0.1.42",
    "typescript": "^6.0.0",
    "vite": "^8.0.0"
  }
}
